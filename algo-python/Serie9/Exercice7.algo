//in the name of Allah

Variables: verb, conj : chaine de character


debut
    repeter
        ecrire("donner un verbe")
        lire(ch)
    jusqu'a (right(ch, 2) = "er")
    ecrire(left(ch, len(ch) - 2), "e")
    ecrire(left(ch, len(ch) - 2), "es")
    ecrire(left(ch, len(ch) - 2), "e")
    ecrire(left(ch, len(ch) - 2), "ion")
    ecrire(left(ch, len(ch) - 2), "ez")
    ecrire(left(ch, len(ch) - 2), "ent")
fin

