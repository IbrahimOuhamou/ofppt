//in the name of Allah

Variables: verb, conj : chaine de character


debut
    repeter
        ecrire("donner un verbe")
        lire(ch)
    jusqu'a (right(ch, 2) = "er")
    ecrire("Je ", left(ch, len(ch) - 2), "e")
    ecrire("Tu ", left(ch, len(ch) - 2), "es")
    ecrire("Il/Elle ", left(ch, len(ch) - 2), "e")
    ecrire("Nous ", left(ch, len(ch) - 2), "ion")
    ecrire("Vous ", left(ch, len(ch) - 2), "ez")
    ecrire("Ils/Elles ", left(ch, len(ch) - 2), "ent")
fin

